// Enable custom clear processed files clear cache entry provided by EXT:toolbar_clear_processedfiles
// Works only for TYPO3 admin users and is only needed if applicationContext !== Development or !== Production/Dev
options.clearCache.processedfiles = 1
